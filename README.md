# 📊 Brand Reputation Intelligence System
### Серверное приложение для сбора и анализа информации о бренде

> **Дипломный проект**
> **Тема:** Проектирование и разработка серверного приложения сбора и анализа информации о бренде в социальных сетях
> **Студент:** Байбеков И.Т. (Группа 4Р2-9.22)

---

## 📝 Описание проекта

Данная система предназначена для автоматизации управления репутацией бренда (ORM). Приложение имитирует сбор данных из популярных источников (ВКонтакте, Telegram, Ozon, Wildberries), проводит автоматический анализ тональности текстов с использованием методов обработки естественного языка (NLP) и визуализирует результаты для принятия управленческих решений.

Проект решает проблему оперативного реагирования на изменения общественного мнения в условиях высокой конкуренции.

### Ключевые возможности
* **NLP Анализ:** Использование нейросети `rubert-tiny-sentiment-balanced` для классификации отзывов (Позитивный / Нейтральный / Негативный).
* **Агрегация данных:** Единый интерфейс для отзывов из разных источников.
* **Симуляция сбора:** Генератор данных для тестирования нагрузки и демонстрации работы алгоритмов.
* **Дашборд:** Визуализация статистики в реальном времени (распределение тональности).
* **REST API:** Архитектура, позволяющая легко интегрировать новые клиенты или источники данных.

---

## 🛠 Технологический стек

### Backend
* **Язык:** Python 3.10+
* **Фреймворк:** Flask (обеспечивает REST API).
* **NLP/ML:** Hugging Face Transformers, PyTorch.
* **Модель:** `cointegrated/rubert-tiny-sentiment-balanced` (оптимизирована под русский язык).
* **База данных:** SQLite (SQLAlchemy ORM).

### Frontend
* **Интерфейс:** HTML5, CSS3, JavaScript (Vanilla).
* **Визуализация:** Chart.js.

---

## 📂 Структура проекта

```text
project/
├── client/                 # Клиентская часть (Frontend)
│   ├── index.html          # Главный дашборд
│   ├── script.js           # Логика взаимодействия с API
│   └── style.css           # Стилизация интерфейса
│
├── server/                 # Серверная часть (Backend)
│   ├── services/
│   │   ├── nlp_service.py  # Модуль инициализации и работы нейросети
│   │   └── generator.py    # Модуль генерации синтетических данных
│   ├── app.py              # Точка входа Flask приложения, роутинг
│   ├── config.py           # Конфигурация приложения и БД
│   ├── models.py           # ORM модели базы данных
│   └── db_init.py          # Скрипт инициализации БД
│
├── requirements.txt        # Зависимости проекта
└── README.md               # Документация


## 🚀 Установка и запуск

### 1. Клонирование репозитория
```bash
git clone [https://github.com/USERNAME/brand-analytics.git](https://github.com/USERNAME/brand-analytics.git)
cd brand-analytics

### 2. Создание виртуального окружения
Windows:
```bash
python -m venv venv
venv\Scripts\activate
macOS / Linux:
```bash
python3 -m venv venv
source venv/bin/activate

### 3. Установка зависимостей
```bash
pip install -r requirements.txt

### 4. Инициализация базы данных
```bash
cd server
python db_init.py

### 5. Запуск сервера
```bash
python app.py

Сервер будет доступен по адресу: http://127.0.0.1:5000/
